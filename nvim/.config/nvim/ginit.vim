set mouse=a

" FIXME Should actually be :h10.5 for the TTF font, replace when distro
" nvim-qt is a version that supports fractional sizes.
Guifont GohuFont:h10

" Copy selections to clipboard automatically
vmap <LeftRelease> "*ygv

set background=dark
colorscheme PaperColor
